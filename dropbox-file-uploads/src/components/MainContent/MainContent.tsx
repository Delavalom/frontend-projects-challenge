import { type FC } from 'react';

type Props = {};

export const MainContent: FC<Props> = ({}) => {

  return (
        <section className="w-3/4 h-full flex items-center justify-center">
          MainContent
        </section>
  );
};
